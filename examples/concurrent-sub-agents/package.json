{
  "name": "@sparkwright/example-concurrent-sub-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/fan-out.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck && node dist/fan-out.js"
  },
  "dependencies": {
    "@sparkwright/agent-runtime": "0.1.0"
  }
}
